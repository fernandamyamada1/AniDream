prefix = "flat"

bash scripts/simple.sh "$prefix an anime plant-based human"
bash scripts/simple.sh "$prefix an anime wilted plant-based human"
bash scripts/simple.sh "$prefix an anime autumn plant-based human"
bash scripts/simple.sh "$prefix an anime plant-based human with blooming buds"

